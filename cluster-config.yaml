# apiVersion: eksctl.io/v1alpha5
# kind: ClusterConfig

# metadata:
#   name: my-eks-cluster
#   region: us-west-2

# nodeGroups:
# - name: ng-1
#   instanceType: t3.medium
#   desiredCapacity: 3
#   ssh:
#     allow: true
#     publicKeyName: my-key-pair


apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: roboshop
  region: us-east-1

managedNodeGroups:
- name: spot
  instanceType: m5.large
  desiredCapacity: 3
  spot: true
